<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid" style="background-color: white;">
        <a class="navbar-brand nav-icon" href="/listings"><i class="fa-brands fa-airbnb"></i> Wanderlust</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav ms-auto order-1 order-md-2 search-dest">
                <form class="d-flex" role="search" method="GET" action="/listings">
                    <input class="form-control e-2 search-inp" type="search" placeholder="Search destinations"
                        value="<%= searchQuery %>" name="search" />
                    <button class="btn search-btn" type="submit"><i
                            class="fa-solid fa-magnifying-glass"></i>Search</button>
                </form>
            </div>
            <div class="navbar-nav order-2 order-md-1">
                <div class="capsule">
                    <a class="nav-link" href="/listings">Explore</a>
                </div>
            </div>
            <div class="navbar-nav ms-auto order-3">
                <div class="capsule">
                    <a class="nav-link" href="/listings/new">Airbnb your home</a>
                </div>
                <% if(!currUser) { %>
                    <div class="capsule"><a class="nav-link" href="/signup">Sign up</a></div>
                    <div class="capsule"><a class="nav-link" href="/login">Log in</a></div>
                    <% } %>
                        <% if(currUser) { %>
                            <div class="capsule"><a class="nav-link" href="/logout">Log out</a></div>
                            <% } %>
            </div>
        </div>
    </div>
</nav>
<script src="/js/search.js"></script>